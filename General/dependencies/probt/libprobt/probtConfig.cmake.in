@PACKAGE_INIT@
include(CMakeFindDependencyMacro)

set(_module_path_save "${CMAKE_MODULE_PATH}")
set(CMAKE_MODULE_PATH "${CMAKE_CURRENT_LIST_DIR}/find_modules")

# Actually we may want to create a target and use include instead of find_dependency TODO(Etienne M)
find_dependency(probt)

set(CMAKE_MODULE_PATH "${_module_path_save}")
unset(_module_path_save)
